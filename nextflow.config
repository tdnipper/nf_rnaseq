params {
    raw_reads = "/home/ubuntu/blockvolume/cappable_seq_rna_seq/raw_data"
    trimmed_reads = "./data/trimmed_reads/*.fastq.gz"
    raw_qc_results = "./results/raw_qc"
}

docker {
    enabled = true
    runOptions = '-u $(id -u):$(id -g)' // Ensures the container runs with the same user ID as the host
}
